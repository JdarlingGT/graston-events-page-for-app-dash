export default function DashboardPage() {
  // Minimal placeholder during build debugging to avoid heavy data collection.
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Dashboard (stubbed)</h1>
      <p className="text-muted-foreground mt-2">Lightweight placeholder to speed up builds while debugging.</p>
    </div>
  );
}